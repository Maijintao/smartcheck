# 智能晨检仪 API 接口文档

## 基础信息

- **服务器地址**: `http://设备IP:8080`
- **协议**: HTTP/HTTPS
- **数据格式**: JSON
- **编码**: UTF-8

---

## 认证方式

所有接口（除登录外）需要在请求头中携带 Token：

```
Authorization: Bearer {token}
```

Token 通过登录接口获取，有效期 24 小时（86400 秒）。

---

## 通用响应格式

### 成功响应

```json
{
  "code": 0,
  "message": "success",
  "data": { ... }
}
```

### 错误响应

```json
{
  "code": 1001,
  "message": "错误描述",
  "data": null
}
```

### 错误码说明

| 错误码 | 说明 |
|-------|------|
| 0 | 成功 |
| 1001 | 未授权（Token 无效或过期） |
| 1002 | 禁止访问 |
| 1003 | 资源不存在 |
| 1004 | 参数错误 |
| 1005 | 服务器内部错误 |
| 1006 | Token 已过期 |
| 2005 | 用户名已存在 |
| 2006 | 数据验证失败 |
| 2009 | 密码不能为空 |
| 2010 | 无效的角色 |
| 2011 | 无效的状态 |

---

## 接口列表

| 序号 | 接口 | 方法 | 功能 |
|:---:|------|:---:|------|
| 1 | `/health` | GET | 健康检查（无需认证） |
| 2 | `/api/auth/login` | POST | 登录获取 Token |
| 3 | `/api/employees` | GET | 获取员工列表 |
| 4 | `/api/employees/sync` | GET | 员工增量同步 |
| 5 | `/api/employees/import` | POST | 批量导入员工（含照片） |
| 6 | `/api/employees/upload-photo` | POST | 上传员工人脸照片 |
| 7 | `/api/employees/upload-cert-photo` | POST | 上传员工健康证照片 |
| 8 | `/api/employees/{employeeId}` | DELETE | 删除员工 |
| 9 | `/api/employees/clear-all` | DELETE | 清空所有员工 |
| 10 | `/api/records` | GET | 批量查询晨检记录 |
| 11 | `/api/records/{id}` | GET | 查询单条记录详情 |
| 12 | `/api/records/sync` | GET | 增量同步 |
| 13 | `/api/records/statistics` | GET | 统计汇总 |
| 14 | `/api/records/export` | POST | 导出 CSV 文件 |
| 15 | `/api/images/{filename}` | GET | 下载晨检图片 |
| 16 | `/api/employee-images/{filename}` | GET | 下载员工照片 |
| 17 | `/api/downloads/{filename}` | GET | 下载导出文件 |
| 18 | `/api/users/sync` | POST | 账号信息同步 |

---

### 1. 健康检查

检查服务是否正常运行，无需认证。

**请求**

```http
GET /health
```

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "status": "ok",
    "timestamp": 1716259200000
  }
}
```

---

### 2. 登录接口

获取访问 Token。

**请求**

```http
POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "123456"
}
```

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "expiresIn": 86400,
    "tokenType": "Bearer",
    "user": {
      "id": 1,
      "username": "admin",
      "name": "管理员"
    }
  }
}
```

---

### 3. 获取员工列表

获取所有员工信息。

**请求**

```http
GET /api/employees?page=1&pageSize=20&employeeId=001&isActive=true
Authorization: Bearer {token}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| page | int | 否 | 页码，默认1 |
| pageSize | int | 否 | 每页条数，默认20，最大100 |
| employeeId | string | 否 | 员工工号，精确匹配 |
| isActive | boolean | 否 | 是否在职（true/false） |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1,
        "name": "张三",
        "employeeId": "001",
        "idCardNumber": "110101199001011234",
        "healthCertStatus": "VALID",
        "healthCertStartDate": 1704067200000,
        "healthCertEndDate": 1735689600000,
        "faceImage": "/api/employee-images/face_001.jpg",
        "healthCertImage": "/api/employee-images/cert_001.jpg",
        "isActive": true,
        "createdAt": 1704067200000
      }
    ],
    "pagination": {
      "page": 1,
      "pageSize": 20,
      "total": 10,
      "totalPages": 1
    }
  }
}
}
```

**健康证状态说明**

| 状态 | 说明 |
|------|------|
| VALID | 有效 |
| EXPIRING_SOON | 即将过期（7天内） |
| EXPIRED | 已过期 |
| NOT_FOUND | 未找到 |

---

### 4. 员工增量同步

用于避免重复拉取数据，只获取新增的员工。

**请求**

```http
GET /api/employees/sync?lastEmployeeId=0&limit=100
Authorization: Bearer {token}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| lastEmployeeId | long | 否 | 上次同步的最后员工ID，默认0 |
| limit | int | 否 | 拉取条数，默认100，最大500 |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [...],
    "hasMore": true,
    "lastRecordId": 10,
    "syncTime": 1706745600000
  }
}
```

---

### 5. 批量导入员工

批量导入员工信息（含照片），支持增量模式和覆盖模式。

**请求**

```http
POST /api/employees/import
Authorization: Bearer {token}
Content-Type: application/json

{
  "employees": [
    {
      "name": "张三",
      "employeeId": "001",
      "idCardNumber": "110101199001011234",
      "faceImageBase64": "/9j/4AAQSkZJRg...",
      "healthCertImageBase64": "/9j/4AAQSkZJRg...",
      "healthCertStartDate": 1704067200000,
      "healthCertEndDate": 1735689600000,
      "isActive": true
    }
  ],
  "incremental": true
}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| employees | array | 是 | 员工列表，最大100条 |
| incremental | boolean | 否 | 是否增量导入，默认 true。<br>true=跳过已存在的员工，false=更新已存在的员工 |
| name | string | 是 | 员工姓名 |
| employeeId | string | 是 | 员工工号（唯一） |
| idCardNumber | string | 否 | 身份证号 |
| phone | string | 否 | 电话 |
| position | string | 否 | 职位 |
| department | string | 否 | 部门 |
| healthCertCode | string | 否 | 健康证编号 |
| faceImageBase64 | string | 否 | 人脸照片 Base64 编码 |
| healthCertImageBase64 | string | 否 | 健康证照片 Base64 编码 |
| healthCertStartDate | long | 否 | 健康证开始日期（时间戳） |
| healthCertEndDate | long | 否 | 健康证结束日期（时间戳） |
| isActive | boolean | 否 | 是否在职，默认 true |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "total": 10,
    "success": 8,
    "failed": 1,
    "details": [
      {
        "employeeId": "001",
        "status": "success",
        "message": "导入成功（含人脸特征）",
        "userId": 1
      },
      {
        "employeeId": "002",
        "status": "failed",
        "message": "员工工号已存在",
        "userId": 2
      },
      {
        "employeeId": "003",
        "status": "skipped",
        "message": "员工工号已存在，跳过",
        "userId": 3
      },
      {
        "employeeId": "004",
        "status": "updated",
        "message": "更新成功",
        "userId": 4
      }
    ]
  }
}
```

**说明**
- `incremental=true`（默认）：跳过已存在的员工，工号相同的记录会标记为 `skipped`
- `incremental=false`：更新已存在的员工信息（姓名、身份证、人脸照片、健康证等），标记为 `updated`
- 同一批次中重复工号会导入失败
- 导入的员工可在员工管理页面查看和编辑
- 人脸照片会自动检测人脸并提取特征，用于人脸识别
- 系统会自动刷新缓存，新员工可直接用于人脸识别

---

### 6. 上传员工人脸照片

为员工上传人脸照片，自动检测人脸并提取特征。

**请求**

```http
POST /api/employees/upload-photo
Authorization: Bearer {token}
Content-Type: application/json

{
  "fileName": "face_001.jpg",
  "imageBase64": "/9j/4AAQSkZJRgABAQAAAQ..."
}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| fileName | string | 是 | 文件名，格式：`face_{工号}.jpg` |
| imageBase64 | string | 是 | 人脸照片 Base64 编码 |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "employeeId": "001",
    "userId": 1,
    "faceImagePath": "face_emp_1716259200000_1.jpg",
    "message": "上传成功，已提取人脸特征"
  }
}
```

**说明**
- 文件名必须严格匹配 `face_{工号}.jpg` 格式
- 系统会自动检测人脸、提取特征、保存图片
- 上传成功后自动刷新人脸识别缓存
- 员工工号必须已存在，不存在则返回 404

---

### 7. 上传员工健康证照片

为员工上传健康证照片。

**请求**

```http
POST /api/employees/upload-cert-photo
Authorization: Bearer {token}
Content-Type: application/json

{
  "fileName": "cert_001.jpg",
  "imageBase64": "/9j/4AAQSkZJRgABAQAAAQ..."
}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| fileName | string | 是 | 文件名，格式：`cert_{工号}.jpg/jpeg/png` |
| imageBase64 | string | 是 | 健康证照片 Base64 编码 |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "employeeId": "001",
    "userId": 1,
    "healthCertImagePath": "cert_emp_1716259200000_1.jpg",
    "message": "上传成功"
  }
}
```

**说明**
- 文件名必须严格匹配 `cert_{工号}.jpg/jpeg/png` 格式
- 员工工号必须已存在，不存在则返回 404

---

### 8. 删除员工

根据工号删除员工。

**请求**

```http
DELETE /api/employees/{employeeId}
Authorization: Bearer {token}
```

**路径参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| employeeId | string | 是 | 员工工号 |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "employeeId": "001",
    "deleted": true
  }
}
```

**说明**
- 删除后该员工无法再进行人脸识别
- 系统会自动刷新缓存

---

### 9. 清空所有员工

删除所有员工数据。

**请求**

```http
DELETE /api/employees/clear-all
Authorization: Bearer {token}
```

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "deleted": true,
    "message": "所有员工已清空"
  }
}
```

**说明**
- 此操作不可恢复，请谨慎使用
- 删除后所有员工无法再进行人脸识别
- 系统会自动刷新缓存

---

### 10. 查询晨检记录列表

按条件查询晨检记录，支持分页和筛选。

**请求**

```http
GET /api/records?startDate=2026-01-01&endDate=2026-01-31&page=1&pageSize=20
Authorization: Bearer {token}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| startDate | string | 是 | 开始日期，格式：yyyy-MM-dd |
| endDate | string | 是 | 结束日期，格式：yyyy-MM-dd |
| employeeId | string | 否 | 员工工号，精确匹配 |
| isPassed | boolean | 否 | 是否通过晨检（true/false） |
| isTempNormal | boolean | 否 | 体温是否正常 |
| isHandNormal | boolean | 否 | 手部是否正常 |
| includeImages | boolean | 否 | 是否包含图片URL（默认false） |
| page | int | 否 | 页码，默认1 |
| pageSize | int | 否 | 每页条数，默认20，最大100 |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "userId": 1,
        "userName": "张三",
        "employeeId": "001",
        "checkTime": 1706745600000,
        "temperature": 36.5,
        "isTempNormal": true,
        "isHandNormal": true,
        "isPassed": true,
        "handStatus": "NORMAL",
        "healthCertStatus": "VALID",
        "symptomFlags": "",
        "remark": "",
        "images": {
          "face": "/api/images/face_20260101_001.jpg",
          "palm": "/api/images/palm_20260101_001.jpg",
          "back": "/api/images/back_20260101_001.jpg"
        }
      }
    ],
    "pagination": {
      "page": 1,
      "pageSize": 20,
      "total": 150,
      "totalPages": 8
    }
  }
}
```

---

### 11. 查询单条记录详情

根据记录 ID 查询详细信息。

**请求**

```http
GET /api/records/{id}?includeImages=true
Authorization: Bearer {token}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| id | long | 是 | 记录ID（路径参数） |
| includeImages | boolean | 否 | 是否包含图片URL（默认true） |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1001,
    "userId": 1,
    "userName": "张三",
    "employeeId": "001",
    "checkTime": 1706745600000,
    "temperature": 36.5,
    "isTempNormal": true,
    "isHandNormal": true,
    "isPassed": true,
    "handStatus": "NORMAL",
    "healthCertStatus": "VALID",
    "symptomFlags": "",
    "remark": "",
    "images": {
      "face": "/api/images/face_20260101_001.jpg",
      "palm": "/api/images/palm_20260101_001.jpg",
      "back": "/api/images/back_20260101_001.jpg"
    }
  }
}
```

---

### 12. 增量同步接口

用于避免重复拉取数据，只获取上次同步后的新记录。

**请求**

```http
GET /api/records/sync?lastRecordId=1000&limit=100
Authorization: Bearer {token}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| lastRecordId | long | 是 | 上次同步的最后一条记录ID |
| limit | int | 否 | 拉取条数，默认100，最大500 |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": 1001,
        "userId": 1,
        "userName": "张三",
        "employeeId": "001",
        "checkTime": 1706745600000,
        "temperature": 36.5,
        "isTempNormal": true,
        "isHandNormal": true,
        "isPassed": true,
        "handStatus": "NORMAL",
        "healthCertStatus": "VALID",
        "symptomFlags": "",
        "remark": ""
      }
    ],
    "hasMore": true,
    "lastRecordId": 1100,
    "syncTime": 1706745600000
  }
}
```

**使用说明**

1. 首次同步：`lastRecordId=0`
2. 保存返回的 `lastRecordId`
3. 下次同步使用上次返回的 `lastRecordId`
4. 如果 `hasMore=true`，继续拉取

---

### 13. 统计汇总接口

获取指定日期范围内的统计数据。

**请求**

```http
GET /api/records/statistics?startDate=2026-01-01&endDate=2026-01-31
Authorization: Bearer {token}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| startDate | string | 是 | 开始日期，格式：yyyy-MM-dd |
| endDate | string | 是 | 结束日期，格式：yyyy-MM-dd |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "totalCheck": 150,
    "passed": 145,
    "failed": 5,
    "tempAbnormal": 2,
    "handAbnormal": 3,
    "dailyStats": [
      {
        "date": "2026-01-01",
        "total": 50,
        "passed": 48,
        "failed": 2,
        "tempAbnormal": 1,
        "handAbnormal": 1
      },
      {
        "date": "2026-01-02",
        "total": 100,
        "passed": 97,
        "failed": 3,
        "tempAbnormal": 1,
        "handAbnormal": 2
      }
    ]
  }
}
```

---

### 14. 导出记录

导出指定日期范围内的记录为 CSV 文件。

**请求**

```http
POST /api/records/export
Authorization: Bearer {token}
Content-Type: application/json

{
  "startDate": "2026-01-01",
  "endDate": "2026-01-31",
  "format": "csv"
}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| startDate | string | 是 | 开始日期，格式：yyyy-MM-dd |
| endDate | string | 是 | 结束日期，格式：yyyy-MM-dd |
| format | string | 是 | 格式：csv（目前仅支持CSV） |
| employeeId | string | 否 | 指定员工工号 |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "downloadUrl": "/api/downloads/records_20260101_20260131.csv",
    "expiresAt": 1706749200000
  }
}
```

---

### 15. 下载图片

下载人脸或手部检测图片。

**请求**

```http
GET /api/images/{filename}
Authorization: Bearer {token}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| filename | string | 是 | 图片文件名（路径参数） |

**响应**

- 成功：返回图片二进制数据（Content-Type: image/jpeg）
- 失败：JSON 错误信息

---

### 16. 下载员工图片

下载员工人脸照片或健康证照片。

**请求**

```http
GET /api/employee-images/{filename}
Authorization: Bearer {token}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| filename | string | 是 | 图片文件名（路径参数） |

**响应**

- 成功：返回图片二进制数据（Content-Type: image/jpeg）
- 失败：JSON 错误信息

---

### 17. 下载导出文件

下载 `export` 接口生成的导出文件，1 小时有效期。

**请求**

```http
GET /api/downloads/records_20260101_20260131.csv
Authorization: Bearer {token}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| filename | string | 是 | 文件名（路径参数） |

**响应**

- 成功：返回文件二进制数据
- 失败：JSON 错误信息（文件不存在或已过期）

**说明**
- 文件名会做安全检查，不支持 `..` `/` `\` 等路径穿越字符
- 文件超过 1 小时未下载会自动过期删除

---

### 18. 账号信息同步接口

同步员工账号信息（登录账号）。

**请求**

```http
POST /api/users/sync
Authorization: Bearer {token}
Content-Type: application/json

{
  "action": "create",
  "users": [
    {
      "username": "001",
      "password": "123456",
      "passwordType": "plain",
      "employeeId": "001",
      "role": "employee",
      "status": "active"
    }
  ]
}
```

**请求参数**

| 参数 | 类型 | 必填 | 说明 |
|-----|------|------|------|
| action | string | 是 | 操作类型：create/update/delete/sync |
| users | array | 是 | 用户列表 |
| username | string | 是 | 登录用户名（工号） |
| password | string | 是 | 密码 |
| passwordType | string | 否 | 密码类型：plain/md5/bcrypt（默认plain） |
| employeeId | string | 否 | 关联员工工号 |
| role | string | 否 | 角色：employee/admin（默认employee） |
| status | string | 否 | 状态：active/disabled（默认active） |

**操作类型说明**

| action | 说明 |
|--------|------|
| create | 新增（如果已存在则跳过） |
| update | 更新（如果不存在则跳过） |
| delete | 删除（如果不存在则跳过） |
| sync | 全量同步（先清空所有账号，再新增） |

**响应**

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "received": 10,
    "success": 10,
    "failed": 0,
    "details": [
      {
        "username": "001",
        "status": "success",
        "message": "同步成功"
      }
    ]
  }
}
```

**错误码**

| 错误码 | 说明 |
|-------|------|
| 0 | 成功 |
| 1004 | 参数错误 |
| 2005 | 用户名已存在 |
| 2006 | 数据验证失败 |
| 2009 | 密码不能为空 |
| 2010 | 无效的角色 |
| 2011 | 无效的状态 |

**完整示例**

```json
// 新增账号
{
  "action": "create",
  "users": [
    {
      "username": "001",
      "password": "123456",
      "employeeId": "001",
      "role": "employee",
      "status": "active"
    },
    {
      "username": "002",
      "password": "123456",
      "employeeId": "002",
      "role": "employee",
      "status": "active"
    }
  ]
}

// 更新密码
{
  "action": "update",
  "users": [
    {
      "username": "001",
      "password": "newpassword888"
    }
  ]
}

// 禁用账号
{
  "action": "update",
  "users": [
    {
      "username": "001",
      "status": "disabled"
    }
  ]
}

// 删除账号
{
  "action": "delete",
  "users": [
    {
      "username": "001"
    }
  ]
}

// 全量同步
{
  "action": "sync",
  "users": [
    {
      "username": "001",
      "password": "123456",
      "employeeId": "001",
      "role": "employee"
    },
    {
      "username": "admin",
      "password": "admin888",
      "role": "admin"
    }
  ]
}
```

---

## 二、App 主动调用云端/平台接口

App 作为 HTTP 客户端，主动调用第三方云端服务器接口。分为两类：

| 类型 | 说明 | Base URL |
|------|------|----------|
| **新平台接入** | 上报晨检记录、设备心跳到客户平台 | 在设备「设置 → 平台接入」中配置 |
| **旧客户兼容** | 从优信系统拉取员工、上报记录 | `http://api.qhk12.iyouxin.cn:50082` |

---

### 2.1 新平台接入接口（推荐）

新平台接入使用 **`api-key`** 方式进行认证。

**认证方式**

| 头字段 | 说明 |
|--------|------|
| `api-key` | 在设备「设置 → 平台接入」中配置的 API Key |

---

#### 2.1.1 设备心跳

设备定期向平台发送心跳，证明设备在线。心跳间隔可在设备设置中配置（默认 30 秒，范围 10~300 秒）。

| 项目 | 值 |
|------|-----|
| 方法 | `POST` |
| 路径 | `/api/device/refresh` |

**请求头**

| 头字段 | 值 |
|--------|-----|
| `api-key` | `{设备配置的API Key}` |

**请求体**

无（空 body）

**响应示例**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "device_id": "DEVICE001",
    "status": "online",
    "last_heartbeat": "2026-06-09T10:00:00Z"
  }
}
```

**响应字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | 状态码，`200` 表示成功 |
| `message` | string | 响应消息 |
| `data.device_id` | string | 设备唯一标识 |
| `data.status` | string | 设备状态 |
| `data.last_heartbeat` | string | 最后心跳时间（ISO 8601 格式） |

---

#### 2.1.2 上报晨检记录

每次晨检完成后，App 自动将记录上报到平台。图片以 Base64（JPEG）编码传输。

| 项目 | 值 |
|------|-----|
| 方法 | `POST` |
| 路径 | `/api/device/morning-check/upload` |

**请求头**

| 头字段 | 值 |
|--------|-----|
| `Content-Type` | `application/json` |
| `api-key` | `{设备配置的API Key}` |

**请求体**

```json
{
  "device_id": "DEVICE001",
  "timestamp": 1716259200000,
  "employees": [
    {
      "id": "E001",
      "name": "张三",
      "temperature": 36.5,
      "photo": "/9j/4AAQSkZJRgABAQAAAQ...",
      "hand_palm_photo": "/9j/4AAQSkZJRgABAQAAAQ...",
      "hand_back_photo": "/9j/4AAQSkZJRgABAQAAAQ..."
    }
  ]
}
```

**字段说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `device_id` | string | 是 | 设备唯一标识 |
| `timestamp` | long | 是 | 检测时间戳（毫秒） |
| `employees` | array | 是 | 员工检测数据列表（当前每次 1 人） |
| `employees[].id` | string | 是 | 员工工号 |
| `employees[].name` | string | 是 | 员工姓名 |
| `employees[].temperature` | float | 是 | 体温（摄氏度） |
| `employees[].photo` | string | 否 | 人脸照片 Base64（JPEG） |
| `employees[].hand_palm_photo` | string | 否 | 手掌照片 Base64（JPEG） |
| `employees[].hand_back_photo` | string | 否 | 手背照片 Base64（JPEG） |

**响应示例**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "recordId": "REC001",
    "processTime": 120,
    "warnings": []
  }
}
```

**响应字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| `code` | int | 状态码，`200` 表示成功 |
| `message` | string | 响应消息 |
| `data.recordId` | string | 平台生成的记录 ID |
| `data.processTime` | int | 处理耗时（毫秒） |
| `data.warnings` | array | 警告信息列表 |

> **注意**：若平台地址或 API Key 未配置，上报会自动跳过。上报失败的数据会保留在本地，网络恢复后自动重试。

---

### 2.2 旧客户兼容接口（优信系统）

以下接口为兼容旧版优信系统保留。

**云端 Base URL**：`http://api.qhk12.iyouxin.cn:50082`

---

#### 2.2.1 查询云端员工列表

| 项目 | 值 |
|------|-----|
| 方法 | `POST` |
| 路径 | `/wosapi/YGCJRobotOpenApi/PageStaff` |

**请求头**

| 头字段 | 值 |
|--------|-----|
| `Content-Type` | `application/json` |
| `yg_sn` | 设备序列号 |

**请求体**

```json
{
  "pageIndex": 0,
  "pageSize": 50,
  "yg_sn": "SN123456789"
}
```

**参数说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `pageIndex` | int | 否 | 页码，从 0 开始 |
| `pageSize` | int | 否 | 每页条数，默认 50 |
| `yg_sn` | string | 否 | 设备序列号 |

**响应示例**

```json
{
  "PageIndex": 0,
  "PageSize": 50,
  "Total": 100,
  "IsSuccess": true,
  "Message": "",
  "DataList": [
    {
      "ThirdKey": "E001",
      "PersonName": "张三",
      "FaceToFacePicUrl": "/upload/face/001.jpg",
      "Phone": "13800138000",
      "Position": "厨师",
      "HCCode": "HC2024001",
      "HCStartTime": "2024-01-01",
      "HCEndTime": "2025-01-01",
      "HCPicUrl": "/upload/cert/001.jpg"
    }
  ]
}
```

**字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| `PageIndex` | int | 当前页码 |
| `PageSize` | int | 每页条数 |
| `Total` | int | 总条数 |
| `IsSuccess` | boolean | 是否成功 |
| `Message` | string | 错误信息 |
| `DataList` | array | 员工列表 |
| `DataList[].ThirdKey` | string | 员工工号（唯一标识） |
| `DataList[].PersonName` | string | 姓名 |
| `DataList[].FaceToFacePicUrl` | string | 人脸照片 URL（相对或绝对路径） |
| `DataList[].Phone` | string | 电话 |
| `DataList[].Position` | string | 职位 |
| `DataList[].HCCode` | string | 健康证编号 |
| `DataList[].HCStartTime` | string | 健康证开始日期 |
| `DataList[].HCEndTime` | string | 健康证到期日期 |
| `DataList[].HCPicUrl` | string | 健康证照片 URL |

> App 拿到 `FaceToFacePicUrl` 和 `HCPicUrl` 后，会再次发起 GET 请求下载图片，并提取人脸特征存入本地数据库。

---

#### 2.2.2 上报晨检记录（旧接口）

每次晨检完成后，App 自动将记录上报到云端。

| 项目 | 值 |
|------|-----|
| 方法 | `POST` |
| 路径 | `/kitchen/morningCheck/saveData` |

**请求头**

| 头字段 | 值 |
|--------|-----|
| `Content-Type` | `application/json` |

**请求体**

```json
{
  "blur": "",
  "deviceToken": "",
  "deviceIp": "192.168.1.100",
  "device_sn": "SN123456789",
  "livenessScore": "",
  "livenessType": 1,
  "passType": 1,
  "personCode": "E001",
  "personId": "",
  "personName": "张三",
  "photo": "/9j/4AAQSkZJRgABAQAAAQ...",
  "recognitionScore": "",
  "recognitionType": 1,
  "timestamp": 1716259200000,
  "verificationMode": 9,
  "temperature": "36.5",
  "temperatureType": 0,
  "maskType": 1,
  "healthyState": 0,
  "cardNumber": "",
  "qrCode": "",
  "idCardNumber": "",
  "serverVerify": "0",
  "verificationType": 0,
  "result": "true",
  "handCheck": {
    "result": "true",
    "handPalmPhoto": "/9j/4AAQSkZJRgABAQAAAQ...",
    "handBackPhoto": "/9j/4AAQSkZJRgABAQAAAQ..."
  }
}
```

**字段说明**

| 字段 | 类型 | 说明 |
|------|------|------|
| `deviceIp` | string | 设备 IP 地址 |
| `device_sn` | string | 设备序列号 |
| `personCode` | string | 员工工号 |
| `personName` | string | 员工姓名 |
| `photo` | string | 人脸照片 Base64 |
| `timestamp` | long | 检测时间戳（毫秒） |
| `temperature` | string | 体温值 |
| `temperatureType` | int | 体温类型：`0` 正常，`1` 异常 |
| `result` | string | 晨检总结果：`true` 通过，`false` 不通过（体温正常且手部正常才通过） |
| `maskType` | int | 口罩状态 |
| `healthyState` | int | 健康状态 |
| `verificationMode` | int | 验证模式，固定 `9` |
| `recognitionType` | int | 识别类型，固定 `1` |
| `livenessType` | int | 活体检测类型，固定 `1` |
| `passType` | int | 通行类型，固定 `1` |
| `handCheck.result` | string | 手部检查结果：`true` / `false` / `unknown` |
| `handCheck.handPalmPhoto` | string | 手掌照片 Base64 |
| `handCheck.handBackPhoto` | string | 手背照片 Base64 |

**响应示例**

```json
{
  "statuCode": 200,
  "info": "success",
  "data": null
}
```

> `statuCode == 200` 表示上报成功。

---

## 数据字段说明

### 晨检记录字段

| 字段 | 类型 | 说明 |
|-----|------|------|
| id | long | 记录唯一ID |
| userId | long | 用户ID |
| userName | string | 用户姓名 |
| employeeId | string | 员工工号 |
| checkTime | long | 检测时间戳（毫秒） |
| temperature | float | 体温（摄氏度） |
| isTempNormal | boolean | 体温是否正常（< 37.3℃） |
| isHandNormal | boolean | 手部是否正常 |
| isPassed | boolean | 是否通过晨检 |
| handStatus | string | 手部状态：NORMAL/FOREIGN_OBJECT/ABNORMAL |
| healthCertStatus | string | 健康证状态：VALID/EXPIRED/NOT_FOUND |
| symptomFlags | string | 症状标记，逗号分隔 |
| remark | string | 备注 |
| images | object | 图片URL对象 |

### 图片对象

| 字段 | 类型 | 说明 |
|-----|------|------|
| face | string | 人脸照片URL |
| palm | string | 手心照片URL |
| back | string | 手背照片URL |

---

## 调用示例

### JavaScript 示例

```javascript
// 1. 登录获取 Token
const loginRes = await fetch('http://192.168.1.100:8080/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username: 'admin', password: '123456' })
});
const { data: { token } } = await loginRes.json();

// 2. 查询记录
const recordsRes = await fetch(
  'http://192.168.1.100:8080/api/records?startDate=2026-01-01&endDate=2026-01-31',
  {
    headers: { 'Authorization': `Bearer ${token}` }
  }
);
const { data } = await recordsRes.json();
console.log(data.list);
```

### Python 示例

```python
import requests

# 1. 登录
login_res = requests.post('http://192.168.1.100:8080/api/auth/login', json={
    'username': 'admin',
    'password': '123456'
})
token = login_res.json()['data']['token']

# 2. 查询记录
records_res = requests.get(
    'http://192.168.1.100:8080/api/records?startDate=2026-01-01&endDate=2026-01-31',
    headers={'Authorization': f'Bearer {token}'}
)
data = records_res.json()['data']
print(data['list'])
```

---

## 注意事项

1. **设备 IP**：实际使用时替换为设备的真实 IP 地址
2. **网络要求**：确保客户系统能访问设备的 8080 端口
3. **Token 有效期**：Token 有效期为 24 小时，过期后需重新登录
4. **时间戳**：所有时间戳单位为毫秒（Java 标准）
5. **分页建议**：单次查询建议不超过 100 条，大数据量请使用增量同步
6. **图片下载**：图片 URL 有效期与 Token 相同，需及时下载
7. **账号同步**：同步账号后，需要在设备上**重新登录**使新账号生效
8. **全量同步**：`action=sync` 会清空所有现有账号，请谨慎使用
9. **平台接入配置**：使用新平台接口前，需在设备「设置 → 平台接入」中配置平台地址和 API Key
10. **心跳间隔**：设备心跳默认 30 秒一次，可在设置中调整（10~300 秒）
11. **上报重试**：网络异常导致上报失败时，数据会保留在本地，恢复后自动补发

---

## 更新日志

| 版本 | 日期 | 说明 |
|-----|------|------|
| 1.8 | 2026-06-09 | **修正 Token 有效期为 24 小时**（原文档误写为 7 天）<br>新增「新平台接入接口」章节（2.1）：<br>— 设备心跳接口 `POST /api/device/refresh`<br>— 晨检记录上报接口 `POST /api/device/morning-check/upload`<br>新增 `api-key` 认证方式说明<br>旧接口归入「旧客户兼容接口」章节（2.2）<br>补充上报接口响应字段说明 |
| 1.7 | 2026-05-22 | 新增健康检查接口 `GET /health`<br>新增上传人脸照片接口 `POST /api/employees/upload-photo`<br>新增上传健康证照片接口 `POST /api/employees/upload-cert-photo`<br>新增下载导出文件接口 `GET /api/downloads/{filename}`<br>新增云端接口章节（查询员工列表、上报晨检记录）<br>补充错误码 2005-2011 |
| 1.6 | 2026-03-06 | 新增删除员工接口 `DELETE /api/employees/{employeeId}`<br>新增清空所有员工接口 `DELETE /api/employees/clear-all`<br>批量导入接口新增 `incremental` 参数支持增量/覆盖模式<br>导入响应增加 skipped/updated 状态详情 |
| 1.5 | 2026-03-05 | 新增员工照片上传接口 `/api/employees/upload-photo`，支持先导入员工再上传照片 |
| 1.4 | 2026-03-05 | 新增批量导入员工接口 `/api/employees/import`，支持人脸照片Base64导入和人脸特征提取 |
| 1.3 | 2026-03-05 | 员工接口新增人脸照片、健康证照片字段；新增 `/api/employee-images` 下载接口 |
| 1.2 | 2026-03-05 | 新增员工信息接口 `/api/employees` 和 `/api/employees/sync` |
| 1.1 | 2026-03-03 | 新增账号同步接口 `/api/users/sync` |
| 1.0 | 2026-03-03 | 初始版本 |
